<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <Solution>$(Solution)</Solution>
    <SolutionTest>$(Solution)_Test</SolutionTest>
    <SolutionPath>$(MSBuildProjectDirectory)\CrossplatLab\$(Solution)\$(Solution).csproj</SolutionPath>
    <SolutionTestPath>$(MSBuildProjectDirectory)\CrossplatLab\$(Solution_Test)\$(Solution_Test).csproj</SolutionTestPath>
  </PropertyGroup>

  <!-- Target for building the project -->
  <Target Name="Build">
    <Exec Command="dotnet build $(SolutionPath)" />
  </Target>

  <!-- Target for running the project -->
  <Target Name="Run">
    <Exec Command="dotnet run --project $(SolutionPath)" />
  </Target>

  <!-- Target for testing the project -->
  <Target Name="Test">
    <Exec Command="dotnet test $(SolutionTestPath)" />
  </Target>
</Project>